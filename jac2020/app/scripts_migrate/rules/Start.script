<%

  parseXml(path("rules.") + "rules.xml");
  parseXml(path("rules.") + "rules.xml");

  int lSize1 = <!rules!>!size();
  int lSize2 = <!rules:rule!>!size();

%>

lSize1 = <!%lSize1!>
lSize2 = <!%lSize2!>

  

